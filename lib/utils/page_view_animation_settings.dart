import 'package:flutter/material.dart';

class PageViewAnimationSettings {
  static const Duration transitionDuration = Duration(microseconds: 270);
  static const Curve transitionCurve = Curves.bounceInOut;
}
